language: php
php:
  - '7.0'
before_script:
  - composer install --prefer-source
  - psql -c 'create database didbot_test;' -U postgres
  - psql -c 'create extension postgis;' -U postgres
script: phpunit
services:
  - postgresql
addons:
  postgresql: "9.4"
